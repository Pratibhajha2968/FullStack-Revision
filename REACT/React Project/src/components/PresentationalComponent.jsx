import React from 'react'

const PresentationalComponent = () => {
  return (
    <div>
      Presentational components
    </div>
  )
}

export default PresentationalComponent


// it focus only on UI.
// no business lofic.
// it uses props.
// it is used for cards , buttons and headers.
// presentational components are responsible only for UI rendering. 
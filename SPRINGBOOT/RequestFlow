/*
      Request flow from frontend to backend :-
               Frontend (React) -> Controller -> Service -> Repository -> Database

                 Frontend (React) :-
                        The user interacts with the UI.
                        React sends an HTTP request (API) call to the backend .


*/

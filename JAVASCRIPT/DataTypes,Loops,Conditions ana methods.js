1. DataTypes :- DataTypes tells which type of data a variable can store.

      DataTypes are of two types:- 
    1.Primitive DataTypes
       a.Num 
       b.String
       c.boolean
       d.double
       e. Null
       f.bigint
    2.Non primitive DataTypes
       a.object
       b.array 
       c.function 
       d.Date , Map set etc.


2.  Loops in js :- Loops are used to execute a block of code  repeatedly until its meet its required condition.

    Types of loops are :-
       1.for loop
         => used when number of counts are known 
       2.while loop
         => used when number of counts are not known
       3.do while loop
        => used to run atleast once 
       4.for-of loop
        => it is the modern or enhanced loop which is used the most for arrays,strings and iterable objects.
       5.for-in loop
         => it is used for object.it iterates over keys.